<div class="container pt-5">
  <h2><%= City.find(params[:id]).name %></h2>
  <p class="mt-3"></p>

  <% @city_gossips.each do |gossip| %>
    <div class="card">
      <div class="card-body">
        <p class="card-text"> <%="#{gossip.title} écrit par #{gossip.user.first_name}"%></p>
        <span class="nav-link"> <%= link_to gossip.id, gossip_path(gossip.id), class: "btn btn-primary" %></span>
      </div>
    </div>
  <% end %>
</div>